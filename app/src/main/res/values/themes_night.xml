<resources xmlns:tools="http://schemas.android.com/tools">
<!-- Base application theme. -->
    <style name="Theme.PosterDark" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary color. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryVariant">@color/light_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/backgroundColor</item>
        <!-- Customize your theme here. -->
        <item name="backgroundColor">@color/black</item>
        <item name="containerColor">@color/light_black</item>
        <item name="hintTextColor">@color/grey</item>
        <item name="textColor">@color/white</item>
        <item name="cardBackground">@drawable/round_square</item>
    </style>

    <style name="Theme.PosterDark.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.PosterDark.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.PosterDark.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>